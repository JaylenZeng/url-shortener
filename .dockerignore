# VCS
.git
.gitignore

# Repo files not copied into the image
*.md
docker-compose.yml
Dockerfile
.dockerignore

# Python / uv artifacts inside the server package
server/.venv
server/**/__pycache__
server/**/*.pyc
server/.pytest_cache
server/.mypy_cache
server/.ruff_cache

# Secrets & tests
server/.env
server/tests/
